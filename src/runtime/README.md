# Runtime Module

This package contains real-time match execution code.

Planned submodules:
- `capture/` for screen capture.
- `perception/` for frame-to-state conversion.
- `policy/` for decision making.
- `actuation/` for keyboard and mouse control.
