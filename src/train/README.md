# Train Module

This package contains model training and dataset pipeline code.

Planned submodules:
- `dataset/` for preprocessing and feature extraction.
- `imitation/` for supervised behavior cloning.
- `evaluation/` for offline training metrics.
