train:
  model_family: imitation_baseline
  batch_size: 32
  learning_rate: 0.0003
  epochs: 10
  train_split: 0.8
  validation_split: 0.2

data:
  processed_dir: data/processed
  export_format: jsonl
